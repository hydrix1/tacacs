#!/bin/bash

exec /usr/local/sbin/tac_plus --print \
    --listen --port=4950 \
	`# spawn = {` \
		`# instances min = 1` \
		`# instances max = 10` \
	`# }` \
	`# background = no` \
\
    --debug="PACKET AUTHEN AUTHOR" \
    --access_log=../output/access_4950.log \
    --accounting_log=../output/acct_4950.log \
    --host=world \
	--address ::/0 \
		`# prompt = \"Welcome\"` \
	--enable=15 --password=secret \
	--key=cisco \
    --group=admin \
	--cmd --permit` \
		`# service = shell {` \
			`# default command = permit` \
			`# default attribute = permit` \
			`# set priv-lvl = 15` \
    --group=guest \
	--cmd --permit \
	--enable --deny \
		`# service = shell {` \
			`# default command = permit` \
			`# default attribute = permit` \
			`# set priv-lvl = 1` \
    --user=cisco \
	--password=cisco \
	--group=admin \
		`# service = shell {` \
		--cmd --permit \
			`# default attribute = permit` \
			`# set priv-lvl = 15` \
    --user=readonly \
	--password=readonly \
	--group=guest \

